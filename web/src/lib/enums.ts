export enum Modals {
  SEARCH = "search",
  NOTIFICATIONS = "notifications",
  FIELD_TRANSFORMATION = "field-transformation",
  TRANSFORMATION_RULE = "transformation-rule",
  // Entity modals (one per type)
  HYDRANT = "hydrant",
  DISPATCH_TYPE = "dispatch-type",
  VIEW_TOKEN = "view-token",
}
